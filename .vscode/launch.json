{
  "version": "0.2.0",
  "inputs": [
    {
      "id": "targetName",
      "description": "Select the target to run",
      "type": "pickString",
      "options": ["Game", "Chess"],
      "default": "Game"
    },
    {
      "id": "presetName",
      "description": "Select the CMake preset",
      "type": "pickString",
      "options": ["windows-x64-debug", "windows-x64-release"],
      "default": "windows-x64-debug"
    }
  ],
  "configurations": [
    {
      "name": "Run (no build)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/${input:presetName}/bin/${input:targetName}/${input:targetName}.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ]
    },
    {
      "name": "Debug (Build + Run)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/${input:presetName}/bin/${input:targetName}/${input:targetName}.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "Build"
    },
    {
      "name": "Debug (Clean Build + Run)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/build/${input:presetName}/bin/${input:targetName}/${input:targetName}.exe",
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "externalConsole": false,
      "MIMode": "gdb",
      "miDebuggerPath": "gdb",
      "setupCommands": [
        {
          "description": "Enable pretty-printing for gdb",
          "text": "-enable-pretty-printing",
          "ignoreFailures": true
        }
      ],
      "preLaunchTask": "Clean Build"
    }
  ]
}
